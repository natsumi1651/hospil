 <div class="body_entire">
  <div class ="body_flex">
   <div class="form_entire">
      <%= form_with(model: @request, local: true) do |form| %>
        <div class ="entry_title">＜予約入力フォーム＞</div>
         <div class="section_title">件名:
          <%= form.text_field :title, placeholder: "『息子がインフルエンザにかかっています。』", class: "entry_section" %>
         </div>
         <div class="section_title">希望日時:
          <%= form.text_field :date, placeholder: "希望日時: 『本日、2023/08/24。』", class: "entry_section" %>
         </div>
         <div class="section_title">カテゴリー:
          <%= form.collection_select(:category_id, Category.all, :id, :name, {}, {class: "category-select"}) %>
         </div>

         <div class="section_title">添付画像(5枚まで投稿できます):<br>
         <div class="upload-ditail">複数アップロードの際は下の画像のように<br>ドラッグをして選択してください。</div>
         <%= image_tag("pic_upload.jpg", alt: "子供部屋", class: "upload-pic") %>
         <%= form.file_field :images, name: 'request[images][]', data: { index: 0 }, id: "request-images", multiple: true %>
         </div>

         <div class="section_title">主訴や、詳細内容を入れてください:
          <%= form.text_area :description, placeholder: "『3歳、男の子です。日曜日に小児科でインフルエンザと言われ、保育所で預かれないと言われました。午後に急用があるため看護をお願いしたいです。』", rows: "20", class: "entry_section" %>
         </div>
          <%# <%= f.collection_select(:category_id, Category.all, :id, :name, {}, {class:"select-box", id:"item-category"}) %> 
          <div class="section_title">希望時間/2時間: 
          <%= form.text_field :expected_length, placeholder: "『今日の午後13時から17時までの看護をお願いしたいです。』", class: "entry_section" %>
          </div>
          <div class="section_title">派遣先住所:
          <%= form.text_area :expected_place, placeholder: "スタッフの派遣先住所を入力してください。マイページの住所の場合は「ユーザー住所」。", rows: "5", class: "entry_section" %>
          </div>
          
          <div class="section_buttonlineup">
              <div class="section_button_customize">
              <%= form.submit "送信", class: "section_button_content_customize" %>
              </div> 
              <div class="section_button">
              <%= link_to "戻る", "/users/#{current_user.id}", class: "section_button_content" %>
              </div>
          </div>

      <% end %>
    </div>
  </div>
</div>